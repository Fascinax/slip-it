export enum GameStatus {
  SETUP = 'SETUP',
  DEALING = 'DEALING',
  IN_PROGRESS = 'IN_PROGRESS',
  FINISHED = 'FINISHED',
}

export enum GameMode {
  POINTS = 'POINTS',
  DRINK = 'DRINK',
  CUSTOM = 'CUSTOM',
}
